// Development / staging environment.
// Point this at the Railway "dev" environment backend once it is provisioned.
// Replace the placeholder URL after running `railway up` in the dev environment.
export const environment = {
  apiUrl: 'https://dochadzkovnik-dev.up.railway.app/api'
};
