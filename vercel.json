{
  "buildCommand": "cd client && npm ci && npm run build:${BUILD_ENV:-prod}",
  "outputDirectory": "client/dist/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
