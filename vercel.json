{
  "buildCommand": "cd client && npm ci && if [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ]; then npm run build:dev; else npm run build:prod; fi",
  "outputDirectory": "client/dist/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
